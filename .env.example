# API base URL — used by the Axios client
# Development (proxied via CRA devserver):
REACT_APP_API_URL=http://localhost:5000/api

# Production (set to your deployed API domain):
# REACT_APP_API_URL=https://api.yourdomainhere.com/api
